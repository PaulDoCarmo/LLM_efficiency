--extra-index-url https://download.pytorch.org/whl/cu128
# Pinné à la même version que le venv principal : torch>=2.1 seul laisse pip
# choisir la dernière version dispo, qui embarque des libs CUDA 13.x
# incompatibles avec le driver de la VM (supporte jusqu'à CUDA 12.8).
# gptqmodel exige torch>=2.7.1 au minimum, 2.11.0 satisfait cette contrainte.
torch==2.11.0+cu128
transformers>=5.4
datasets>=2.19,<4
# Pas de plafond sur huggingface_hub ici (contrairement à requirements.txt/
# requirements-awq.txt) : transformers>=5.4 exige huggingface-hub>=1.5,<2.0.
accelerate>=0.33
# Plafond <7.3.5 : les versions 7.3.5+ demandent transformers>=5.14 au lieu de
# >=5.4, inutilement plus strict pour ce qu'on en fait ici.
gptqmodel>=7.0,<7.3.5
optimum>=1.20  # transformers en a besoin comme pont pour charger un modèle GPTQ
# gptqmodel importe sans condition tous les modèles qu'il supporte (y compris
# des modèles vision-langage type InternVL), qui ont besoin de torchvision
# même si on ne charge que du Qwen2 texte.
torchvision
ninja  # requis pour la compilation JIT du noyau Marlin de gptqmodel
# lm-eval (--ifeval) n'a pas été testé avec transformers>=5.4 — ARC (--arc)
# n'en dépend pas du tout et est le choix sûr ici. Essayer --ifeval est
# possible mais peut échouer si lm-eval-harness ne supporte pas encore
# transformers 5.x.
lm-eval[ifeval]>=0.4.4
numpy
pandas
