--extra-index-url https://download.pytorch.org/whl/cu128
# Pinné à la même version que le venv principal : torch>=2.1 seul laisse pip
# choisir la dernière version dispo, qui embarque des libs CUDA 13.x
# incompatibles avec le driver de la VM (supporte jusqu'à CUDA 12.8).
torch==2.11.0+cu128
# autoawq est officiellement abandonné (message affiché à l'import) ; son
# code importe des architectures (ex: Qwen3) absentes de transformers<=4.47.1
# malgré ce que ses métadonnées PyPI déclarent. Sa propre note de dépréciation
# indique 4.51.3 comme dernière combinaison testée par son mainteneur.
transformers==4.51.3
datasets>=2.19,<4
huggingface_hub<1.0
accelerate>=0.33
autoawq>=0.2
lm-eval[ifeval]>=0.4.4
numpy
pandas
